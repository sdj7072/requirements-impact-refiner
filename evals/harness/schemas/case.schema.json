{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Installed plugin evaluation case",
  "oneOf": [
    { "$ref": "#/$defs/singleTurnCase" },
    { "$ref": "#/$defs/lineageCase" }
  ],
  "$defs": {
    "rubric": {
      "type": "array",
      "items": { "type": "string", "minLength": 1, "pattern": "\\S" }
    },
    "fixtureFile": {
      "type": "object",
      "required": ["path", "content"],
      "additionalProperties": false,
      "properties": {
        "path": { "type": "string", "minLength": 1, "maxLength": 4096, "pattern": "\\S" },
        "content": { "type": "string", "minLength": 1, "maxLength": 65536, "pattern": "\\S" }
      }
    },
    "fixtureFiles": {
      "type": "array",
      "maxItems": 32,
      "items": { "$ref": "#/$defs/fixtureFile" }
    },
    "turn": {
      "type": "object",
      "required": ["prompt", "repository_evidence"],
      "additionalProperties": false,
      "properties": {
        "prompt": { "type": "string", "minLength": 1, "pattern": "\\S" },
        "repository_evidence": { "$ref": "#/$defs/rubric" }
      }
    },
    "common": {
      "type": "object",
      "required": ["id", "kind", "must_detect", "must_not_do", "modes", "fixture_files"],
      "properties": {
        "id": { "type": "string", "minLength": 1, "pattern": "\\S" },
        "must_detect": { "$ref": "#/$defs/rubric" },
        "must_not_do": { "$ref": "#/$defs/rubric" },
        "fixture_files": { "$ref": "#/$defs/fixtureFiles" },
        "modes": {
          "type": "array",
          "items": {
            "enum": ["generic", "codex", "claude-code", "superpowers", "claude-feature-dev", "spec-kit"]
          }
        }
      }
    },
    "singleTurnCase": {
      "allOf": [
        { "$ref": "#/$defs/common" },
        {
          "type": "object",
          "required": ["request", "repository_evidence"],
          "additionalProperties": false,
          "properties": {
            "id": { "type": "string", "minLength": 1, "pattern": "\\S" },
            "kind": { "enum": ["positive", "negative", "integration"] },
            "request": { "type": "string", "minLength": 1, "pattern": "\\S" },
            "repository_evidence": { "$ref": "#/$defs/rubric" },
            "must_detect": { "$ref": "#/$defs/rubric" },
            "must_not_do": { "$ref": "#/$defs/rubric" },
            "fixture_files": { "$ref": "#/$defs/fixtureFiles" },
            "modes": { "$ref": "#/$defs/common/properties/modes" }
          }
        }
      ]
    },
    "lineageCase": {
      "allOf": [
        { "$ref": "#/$defs/common" },
        {
          "type": "object",
          "required": ["turns", "expected_transition"],
          "additionalProperties": false,
          "properties": {
            "id": { "type": "string", "minLength": 1, "pattern": "\\S" },
            "kind": { "const": "lineage" },
            "turns": {
              "type": "array",
              "minItems": 2,
              "maxItems": 2,
              "items": { "$ref": "#/$defs/turn" }
            },
            "must_detect": { "$ref": "#/$defs/rubric" },
            "must_not_do": { "$ref": "#/$defs/rubric" },
            "fixture_files": { "$ref": "#/$defs/fixtureFiles" },
            "modes": { "$ref": "#/$defs/common/properties/modes" },
            "expected_transition": { "enum": ["unchanged", "reopened", "rejected"] }
          }
        }
      ]
    }
  }
}
